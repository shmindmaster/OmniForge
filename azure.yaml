name: im2fit
infra:
  provider: bicep
services:
  api:
    project: .
    language: python
    host: appservice
    docker:
      path: .
      file: Dockerfile
    env:
  USE_ONNX: 0
  PUBLIC_ARTIFACT_PROXY: 1
  STORAGE_CONTAINER: artifacts
  BLOB_CONTAINER: artifacts
  APP_ENV: demo
